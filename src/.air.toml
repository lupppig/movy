root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main ."
  bin = "./tmp/main"
  include_ext = ["go", "yaml"]
  exclude_dir = ["tmp", "vendor"]
  include_dir = []
  exclude_file = []
  log = "air.log"
  delay = 1000
  stop_on_error = true
